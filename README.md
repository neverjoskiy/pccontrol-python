# Advanced Telegram Bot для Windows

Полнофункциональный Telegram-бот для удалённого управления компьютером с Windows 10/11.

## 📋 Возможности

### Process Manager
- Просмотр запущенных процессов с информацией о RAM/CPU
- Завершение процессов (Kill)
- Перезапуск процессов (Restart)
- Изменение приоритета процессов
- Запуск новых процессов
- Мониторинг конкретных процессов

### File Explorer
- Навигация по всем дискам (C:\, D:\, etc.)
- Просмотр содержимого папок
- Скачивание файлов (до 50MB)
- Удаление файлов и папок
- Переименование
- Перемещение и копирование
- Загрузка файлов в текущую директорию

### System Control
- Выключение ПК
- Перезагрузка ПК
- Спящий режим
- Блокировка системы
- Скриншот экрана
- Снимок с веб-камеры
- Запись аудио с микрофона
- Просмотр буфера обмена

### Network Tools
- Показ публичного и локального IP
- Ping хостов
- Netstat (активные соединения)
- DNS Lookup
- Speedtest интернета

### Monitoring
- Мониторинг загрузки CPU
- Мониторинг использования RAM
- Отслеживание изменений в директориях

### Extra Tools
- Выполнение CMD команд
- Кейлоггер (вкл/выкл)
- Установка буфера обмена
- Аудиозапись
- Управление автозагрузкой

## 🚀 Установка

### 1. Установка Python
Убедитесь, что установлен Python 3.9 или выше:
```
python --version
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Получение токена бота
1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

### 4. Настройка токена

**Способ 1: Через переменную окружения (рекомендуется)**
```bash
setx TELEGRAM_BOT_TOKEN "your_token_here"
```
После этого перезапустите терминал.

**Способ 2: Через файл .env**
1. Скопируйте `.env.example` в `.env`
2. Откройте `.env` и вставьте токен:
```
TELEGRAM_BOT_TOKEN=your_token_here
```

### 5. Запуск бота
```bash
python main.py
```

## 🔐 Первая авторизация

1. Запустите бота
2. В Telegram отправьте боту команду `/start`
3. По умолчанию пароль: `change_me`
4. Отправьте команду `/auth` и введите пароль
5. После успешной аутентификации ваш user ID будет добавлен в разрешённые

**Важно:** Сразу измените пароль через меню Settings → Change Password!

## 📁 Структура проекта

```
full control/
├── main.py              # Точка входа
├── bot.py               # Основной обработчик
├── keyboards.py         # Inline-клавиатуры
├── security.py          # Модуль безопасности
├── process_manager.py   # Управление процессами
├── file_manager.py      # Файловый менеджер
├── system_manager.py    # Управление системой
├── network_manager.py   # Сетевые инструменты
├── monitor.py           # Мониторинг
├── config.json          # Конфигурация
├── requirements.txt     # Зависимости
├── .env.example         # Пример .env файла
└── README.md            # Эта инструкция
```

## ⚙️ Конфигурация

Файл `config.json` хранит:
- `allowed_user_ids` - список разрешённых Telegram user ID
- `password` - пароль для аутентификации
- `current_directories` - текущие директории пользователей
- `monitored_paths` - отслеживаемые пути
- `keylogger_active` - статус кейлоггера
- `startup_enabled` - статус автозагрузки

## 🛡️ Безопасность

- Только авторизованные пользователи могут управлять ботом
- Пароль требуется при первой аутентификации
- Все действия логируются в `bot.log`
- Токен бота не должен храниться в config.json

## 📝 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Показать главное меню |
| `/help` | Показать справку |
| `/status` | Показать статус системы |
| `/auth` | Аутентификация по паролю |
| `/cancel` | Отменить текущее действие |

## 🔧 Дополнительные зависимости

Некоторые функции требуют дополнительных библиотек:

### Для скриншотов (если Pillow не работает):
```bash
pip install Pillow
```

### Для веб-камеры:
```bash
pip install opencv-python
```

### Для записи аудио:
```bash
pip install pyaudio
```
Если возникнут проблемы с установкой PyAudio на Windows:
```bash
pip install pipwin
pipwin install pyaudio
```

### Для speedtest:
```bash
pip install speedtest-cli
```

## ⚠️ Важные замечания

1. **Ограничение на файлы:** Telegram ограничивает размер файлов 50MB для ботов
2. **Права доступа:** Некоторые функции требуют прав администратора
3. **Безопасность:** Не запускайте бота на критически важных системах без дополнительной защиты
4. **Логирование:** Все действия записываются в `bot.log`

## 🐛 Решение проблем

### Бот не запускается
- Проверьте токен в `.env` или переменной окружения
- Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`

### "Access Denied"
- Ваш user ID не в списке разрешённых
- Используйте `/auth` с правильным паролем

### Функция не работает
- Проверьте права администратора
- Проверьте логи в `bot.log`

### Ошибка при установке pyaudio
```bash
pip install pipwin
pipwin install pyaudio
```

## 📄 Лицензия

Используйте на свой страх и риск. Только для образовательных целей и личного использования.
