# 🚀 Запуск системы с веб-интерфейсом

## Быстрый старт

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Запуск бота с веб-интерфейсом
```bash
python main.py
```

Система запустит **одновременно**:
- 🤖 **Telegram Bot** - управление через Telegram (@YourBotName)
- 🌐 **Web Interface** - управление через браузер (http://localhost:5000)

## 📱 Использование веб-интерфейса

### Вход в систему
1. Откройте браузер: `http://localhost:5000`
2. Введите пароль (по умолчанию: `change_me`)
3. Вы попадёте на Dashboard

### Загрузка файлов
**Способ 1: Drag & Drop**
- Просто перетащите файлы в зону загрузки
- Поддерживается множественная загрузка

**Способ 2: Клик по зоне**
- Нажмите на зону загрузки
- Выберите файлы через диалог

### Управление файлами
- 📥 **Скачать** - нажмите кнопку Download рядом с файлом
- 🗑️ **Удалить** - нажмите кнопку Delete (требует подтверждение)

## 🔒 Безопасность

- Веб-интерфейс доступен **только локально** (localhost:5000)
- Требуется **аутентификация паролем** для входа
- Все действия **логируются** в bot.log
- Поддерживаются расширения файлов в whitelist

## 📊 Мониторинг

Dashboard показывает реальную информацию:
- **CPU Load** - загрузка процессора в %
- **RAM Usage** - использование оперативной памяти (GB и %)
- **Disk Usage** - использование диска (GB и %)
- Информация автоматически обновляется каждые 5 секунд

## ⚙️ Конфигурация

Отредактируйте `config.json` для изменения:
```json
{
  "allowed_user_ids": [123456789],
  "password": "change_me",
  ...
}
```

Папка загрузок: `uploads/` (в корне проекта)
Максимальный размер файла: 500 MB

## 🔑 Смена пароля

**Способ 1: Через Telegram**
- Отправьте /start
- Settings → Change Password
- Следуйте инструкциям

**Способ 2: Прямо в config.json**
- Отредактируйте значение "password"
- Перезагрузите приложение

## ❌ Решение проблем

### "Connection refused" при открытии http://localhost:5000
- Проверьте, что бот запущен (должны быть сообщения в консоли)
- Очистите кэш браузера (Ctrl+Shift+Delete)
- Попробуйте другой браузер

### "Неверный пароль" при входе
- Используйте пароль из config.json (по умолчанию `change_me`)
- Проверьте, не содержит ли пароль пробелы в начале/конце

### Файл не загружается
- Проверьте расширение (поддерживаемые: doc, exe, zip, jpg, mp4, и другие)
- Убедитесь, что файл меньше 500 MB
- Проверьте права доступа к папке uploads/

## 📝 Логирование

Все события логируются в `bot.log`:
```
✓ File uploaded: document.pdf
✓ File downloaded: image.jpg
✓ File deleted: old.zip
✓ User logged in via web interface
```

Проверяйте логи при возникновении проблем!

## 🎯 Дальше

- Telegram бот по-прежнему работает как обычно
- Оба интерфейса используют одну конфигурацию (config.json)
- Вы можете использовать Telegram И веб-интерфейс одновременно!
