# ⚡ ПЕРВЫЙ ЗАПУСК - Веб интерфейс и загрузка файлов

## ✅ Что было добавлено

1. **Веб-интерфейс Flask** (`web_app.py`)
   - Красивый Dashboard с статистикой
   - Аутентификация паролем
   - Live обновление показателей CPU/RAM/Disk каждые 5 секунд

2. **Загрузка файлов**
   - Drag & Drop поддержка 
   - Загрузка сразу в папку `/uploads`
   - Просмотр, скачивание и удаление файлов
   - Поддержка файлов до 500 MB

3. **HTML/CSS шаблоны** (папка `templates/`)
   - `login.html` - страница входа с красивым дизайном
   - `dashboard.html` - главная страница с управлением
   - `error.html` - страница ошибок

4. **Интеграция** 
   - Веб-сервер запускается в отдельном потоке
   - Работает параллельно с Telegram ботом
   - Использует один и тот же пароль из config.json

## 🚀 Запуск

```bash
# Установка зависимостей (если еще не установлены)
pip install -r requirements.txt

# Запуск (будут работать оба: Telegram бот + веб-интерфейс)
python main.py
```

После запуска откройте в браузере:
```
http://localhost:5000
```

Пароль: `change_me` (или измените в config.json)

## 🎨 Возможности веб-интерфейса

### Dashboard
- 📊 CPU, RAM, Disk статистика в реальном времени
- 💻 Информация о системе (CPU cores, RAM total, etc.)

### Файлы
- 📤 **Загрузить** - перетащите или нажмите для выбора
- 📥 **Скачать** - нажмите Download рядом с файлом
- 🗑️ **Удалить** - нажмите Delete (с подтверждением)

### Все файлы сохраняются в: `/uploads`

## 📚 Подробнее

Смотрите `WEB_INTERFACE_GUIDE.md` для полной документации!

---

**Готово! Веб-интерфейс готов к использованию!** ✨
